package com.revify.monolith.commons.messaging;

public interface ExchangeBindings {
    //client.composite. + countryName + cityName lowercase + .#
    String CLIENT_COMPOSITE = "client.composite.";
}
